import React from "react";

// Style
import "./Button.css";

const Button = () => {
  return (
    <div>
      <button className="btn-primary">Generate New Array</button>
    </div>
  );
};

export default Button;
